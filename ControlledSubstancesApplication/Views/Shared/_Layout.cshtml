<!DOCTYPE html>
<html>
<head>
    @{
        System.Diagnostics.Stopwatch sw = new System.Diagnostics.Stopwatch();
        sw.Start();
    }
    <meta charset="utf-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>@ViewBag.Title - Controlled Substances Log</title>
    @Styles.Render("~/Content/css")
    @Styles.Render("~/Content/jquery-css")
    @Scripts.Render("~/bundles/modernizr")
    @Scripts.Render("~/bundles/jquery")
    @Scripts.Render("~/bundles/bootstrap")
    @Scripts.Render("~/bundles/jquery-ui")
    @Scripts.Render("~/bundles/jqueryval")
    @RenderSection("scripts", required: false)
    @if (IsSectionDefined("AddToHead"))
    {
        @RenderSection("AddToHead", required: false)
    }
      <!-- HTML5 Shim and Respond.js IE8 support of HTML5 elements and media queries -->
        <!--[if lt IE 9]>
            @Scripts.Render("~/bundles/ie8scripts")
        <![endif]-->
</head>
<body>
<div class="topbar">&nbsp;</div>
<!--[if lt IE 8]>
<div class="ie-warning">Unsupported browser.  For the best experience, please consider using Google Chrome or Firefox.</div>


<!--<![endif]-->
    <div class="container body-content">
        <div class="header">
            <div style="width:350px; float:left;"><img src="~/Images/summit-logo-website.png" /></div>
            <div style="margin-left:50px; float:left;"><h2>Controlled Substance Log</h2></div>
            <div class="clearfix"></div>
        </div>
        <hr />

        @{ 
            /* if (Request.Browser.Browser == "IE" || Request.Browser.Browser == "InternetExplorer")
            {
                <div class="general-warning"><p>NOTE:  The Controlled Substance Log is not currently functional in Internet Explorer.</p>
                    <p>Please use another browser such as Chrome or Firefox while we work to fix the issue.  Otherwise, please keep entries on a paper log
                    until the issue is resolved.</p>
                    
                    <p>If you require assistance, please contact the help desk at (865) 212-4148</p>
                </div>
            }
            else
            { */
                if (System.Configuration.ConfigurationManager.ConnectionStrings["Db"].ConnectionString.Contains("vs-dev-db"))
                {
                    <div class="test-db-warning">APPLICATION IS USING TEST DATABASE</div><br />
                }
                @RenderBody();
            //}
        }

        <p>&nbsp;</p>
        <p>&nbsp;</p>
        <p>&nbsp;</p>
        <footer id="footer" style="position:absolute; width:97%; text-align:center; bottom:0;">
            @{Html.RenderPartial("~/Views/Partials/Footer.cshtml");}
        </footer>
    </div>
    @{ sw.Stop();
        if (HttpContext.Current.IsDebuggingEnabled)
        {
            <p>@sw.Elapsed</p>
        }
    }


</body>
</html>